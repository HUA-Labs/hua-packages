name: Release SDK

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build publishable packages
        run: pnpm build --filter "@hua-labs/*" --filter "!@hua-labs/pro" --filter "!@hua-labs/ui-dashboard" --filter "!@hua-labs/docs-engine" --filter "!@hua-labs/encryption" --filter "!@hua-labs/security" --filter "!@hua-labs/api-lite"

      - name: Create .npmrc for npm token
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > .npmrc
          echo "access=public" >> .npmrc
          echo "registry=https://registry.npmjs.org/" >> .npmrc
          echo "provenance=true" >> .npmrc

      - name: Create release (changesets)
        uses: changesets/action@v1
        with:
          publish: pnpm changeset publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
