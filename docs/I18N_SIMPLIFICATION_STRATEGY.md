# HUA Platform - i18n SDK 단순화 전략

## 🎯 **문제 상황**

### **현재 상황**
- **@hua-labs/i18n-sdk** - `1.2.2` ✅ (이미 퍼블리시됨)
  - GitHub: `https://github.com/HUA-Labs/i18n-sdk`
  - npm: 이미 등록됨
  - **모든 기능 통합**: easy, beginner, advanced, debug, ai 등

### **모노레포의 중복 패키지들**
```bash
packages/
├── hua-i18n-sdk/          # ✅ 이미 퍼블리시됨
├── hua-i18n-core/         # ❌ 불필요 (중복)
├── hua-i18n-beginner/     # ❌ 불필요 (중복)
├── hua-i18n-advanced/     # ❌ 불필요 (중복)
├── hua-i18n-ai/           # ❌ 불필요 (중복)
├── hua-i18n-debug/        # ❌ 불필요 (중복)
└── hua-i18n-plugins/      # ❌ 불필요 (중복)
```

## 🚀 **해결 전략: 단순화**

### **핵심 아이디어**
- **기존 `@hua-labs/i18n-sdk`만 사용**
- **모노레포의 중복 패키지들 제거**
- **모든 앱에서 동일한 i18n-sdk 사용**

### **장점**
1. **스코프 충돌 해결**: 기존 스코프 패키지만 사용
2. **중복 제거**: 불필요한 패키지들 제거
3. **일관성**: 모든 앱에서 동일한 i18n SDK
4. **단순함**: 복잡한 패키지 구조 제거

## 📦 **퍼블리시 대상 (단순화)**

### **✅ 이미 퍼블리시됨**
1. **@hua-labs/i18n-sdk** - `1.2.2`
   - 모든 i18n 기능 통합
   - 모드별 엔트리포인트 제공

2. **@hua-labs/api-lite** - `2.1.2`
   - API 클라이언트 SDK

### **🆕 새로 퍼블리시할 것들**
1. **@hua-labs/animation** - `0.1.0`
   - 범용 애니메이션 SDK

2. **@hua-labs/ui** - `0.1.0`
   - UI 컴포넌트 라이브러리

## 🔄 **실행 계획**

### **1단계: 중복 패키지 제거**
```bash
# 제거할 패키지들
rm -rf packages/hua-i18n-core
rm -rf packages/hua-i18n-beginner
rm -rf packages/hua-i18n-advanced
rm -rf packages/hua-i18n-ai
rm -rf packages/hua-i18n-debug
rm -rf packages/hua-i18n-plugins
```

### **2단계: 의존성 통일**
```json
// 모든 앱의 package.json에서
{
  "dependencies": {
    "@hua-labs/i18n-sdk": "^1.2.2"  // 기존 스코프 패키지 사용
  }
}
```

### **3단계: import 경로 통일**
```typescript
// 기존 (중복 패키지)
import { useTranslation } from '@hua-labs/i18n-beginner'

// 변경 후 (통합 패키지)
import { useTranslation } from '@hua-labs/i18n-sdk/beginner'
```

### **4단계: npm 퍼블리시**
```bash
# 애니메이션 SDK
cd packages/hua-animation
npm publish

# UI SDK
cd packages/hua-ui
npm publish
```

## 📁 **최종 패키지 구조**

```bash
hua-platform/
├── packages/
│   ├── hua-animation/     # 🆕 퍼블리시 예정
│   ├── hua-ui/           # 🆕 퍼블리시 예정
│   ├── hua-i18n-sdk/     # ✅ 이미 퍼블리시됨 (workspace:*)
│   └── hua-my-api-sdk/  # ✅ 이미 퍼블리시됨
├── apps/
│   ├── hua-animation-site/
│   ├── hua-ui-site/
│   └── ...
└── package.json
```

## 🎯 **사용법 (통일)**

### **모든 앱에서 동일한 방식**
```typescript
// 초보자용
import { useTranslation } from '@hua-labs/i18n-sdk/beginner'

// 고급자용
import { useTranslation } from '@hua-labs/i18n-sdk/advanced'

// AI 기능
import { useTranslation } from '@hua-labs/i18n-sdk/ai'
```

## 💡 **이 전략의 장점**

### **✅ 단순함**
- 스코프 충돌 없음
- 중복 패키지 제거
- 명확한 구조

### **✅ 일관성**
- 모든 앱에서 동일한 i18n SDK
- 통일된 API 사용법
- 버전 관리 단순화

### **✅ 유지보수성**
- 하나의 i18n 패키지만 관리
- 업데이트 시 모든 앱에 자동 적용
- 문서화 단순화

---

**마지막 업데이트**: 2025년 1월
**상태**: 전략 수립 완료 ✅ 