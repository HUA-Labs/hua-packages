# HUA Platform - 단순화된 퍼블리시 전략

## 🎯 **전략: 모노레포 유지 + 개별 퍼블리시**

### **핵심 아이디어**
- **로컬**: 모노레포 구조 유지 (개발 효율성)
- **npm**: 각 패키지를 개별적으로 퍼블리시
- **GitHub**: 모노레포 리포 하나로 관리

## 📦 **퍼블리시 대상**

### **✅ 이미 퍼블리시됨**
1. **@hua-labs/i18n-sdk** - `1.2.2`
   - GitHub: `https://github.com/HUA-Labs/i18n-sdk`
   - npm: ✅ 등록됨

2. **@hua-labs/api-lite** - `2.1.2`
   - GitHub: `https://github.com/HUA-Labs/hua-sdk-lite`
   - npm: ✅ 등록됨

### **🆕 새로 퍼블리시할 것들**
1. **@hua-labs/animation** - `0.1.0`
   - 범용 애니메이션 SDK (React, Vue, 바닐라 JS)
   - 외부 의존성 없음

2. **@hua-labs/ui** - `0.1.0`
   - UI 컴포넌트 라이브러리
   - React 18/19 호환

## 🚀 **퍼블리시 단계**

### **1단계: npm 로그인**
```bash
npm login
# npm 계정으로 로그인
```

### **2단계: 애니메이션 SDK 퍼블리시**
```bash
cd packages/hua-animation
npm publish
```

### **3단계: UI SDK 퍼블리시**
```bash
cd packages/hua-ui
npm publish
```

### **4단계: 사이트 배포**
```bash
# Vercel에서 hua-animation-site 배포
# Vercel에서 hua-ui-site 배포
```

## 🔄 **개발 워크플로우**

### **일상 개발**
```bash
# 모노레포에서 개발
cd hua-platform
pnpm dev
```

### **버전 업데이트 시**
```bash
# 애니메이션 SDK 업데이트
cd packages/hua-animation
npm version patch
npm publish

# UI SDK 업데이트
cd packages/hua-ui
npm version patch
npm publish
```

## 📁 **파일 구조 (변경 없음)**

```
hua-platform/
├── packages/
│   ├── hua-animation/     # 🆕 퍼블리시 예정
│   ├── hua-ui/           # 🆕 퍼블리시 예정
│   ├── hua-i18n-sdk/     # ✅ 이미 퍼블리시됨
│   └── hua-my-api-sdk/  # ✅ 이미 퍼블리시됨
├── apps/
│   ├── hua-animation-site/  # 🆕 배포 예정
│   ├── hua-ui-site/         # 🆕 배포 예정
│   └── ...
└── package.json
```

## 🌐 **사이트 배포**

### **Vercel 배포**
1. **hua-animation-site**
   - 도메인: `animation.hua-labs.com` (예시)
   - 내용: 애니메이션 SDK 문서, 예제, 벤치마크

2. **hua-ui-site**
   - 도메인: `ui.hua-labs.com` (예시)
   - 내용: UI 컴포넌트 문서, 플레이그라운드

## ⚡ **빠른 시작**

### **1. npm 퍼블리시**
```bash
# 애니메이션 SDK
cd packages/hua-animation
npm publish

# UI SDK
cd packages/hua-ui
npm publish
```

### **2. Vercel 배포**
```bash
# GitHub 리포를 Vercel에 연결
# 자동 배포 설정
```

## 🎉 **퍼블리시 완료 후**

### **npm 패키지**
```bash
npm install @hua-labs/animation
npm install @hua-labs/ui
```

### **사이트 접속**
- 애니메이션 문서: `https://animation.hua-labs.com`
- UI 문서: `https://ui.hua-labs.com`

## 💡 **이 전략의 장점**

### **✅ 단순함**
- 복잡한 리포 분리 없음
- 기존 구조 그대로 유지
- 바로 퍼블리시 가능

### **✅ 개발 효율성**
- 모노레포의 장점 그대로 활용
- 패키지 간 의존성 쉽게 관리
- 통합 개발 환경

### **✅ 일관성**
- 기존 i18n-sdk와 동일한 방식
- 팀원들이 익숙한 워크플로우

---

**마지막 업데이트**: 2025년 1월
**상태**: 전략 수립 완료 ✅ 