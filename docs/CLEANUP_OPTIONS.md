# 코드베이스 정리 옵션 분석

## 현재 상태

### 빌드 성공 ✅
- `my-api` - 메인 프로덕션 앱
- `my-chat` - 메인 프로덕션 앱

### 빌드 실패 ❌
- `my-app` - 메인 프로덕션 앱 (타입 에러)
- `i18n-test` - 개발/테스트 앱 (타입 에러)
- `hua-motion` - 개발/테스트 앱 (빌드 에러)
- `hua-demo` - 데모 앱 (SUM API 데모)
- `hua-labs` - 데모 앱 (기본 Next.js 템플릿)
- `hua-ui` - 문서/데모 앱 (UI 패키지 사이트)

## 옵션 1: 지우기 (Cleanup)

### 삭제 대상 후보
1. **`hua-labs`** - 기본 Next.js 템플릿, 실제 사용 안 함
2. **`hua-demo`** - SUM API 데모, 필요시 재생성 가능
3. **`hua-ui`** - UI 패키지 사이트, Storybook으로 대체 가능

### 장점 ✅
- 코드베이스 정리 및 단순화
- 빌드 시간 단축
- 유지보수 부담 감소
- CI/CD 파이프라인 단순화
- 의존성 관리 용이

### 단점 ❌
- 나중에 필요할 수 있음
- 삭제 후 복구 어려움
- 데모/문서 목적 상실

### 추천 삭제 대상
- `hua-labs` - 기본 템플릿, 명확히 불필요
- `hua-demo` - 필요시 재생성 가능

### 보류 대상
- `hua-ui` - Storybook과 중복이지만 UI 패키지 문서화 목적
- `i18n-test`, `hua-motion` - 개발 중인 기능 테스트용

## 옵션 2: 빌드/린트 성공시키기 (Fix All)

### 수정 대상
1. **`my-app`** - `nickname_hash` 타입 에러 수정
2. **`i18n-test`** - Window 타입 확장
3. **`hua-motion`** - 빌드 에러 수정
4. **`hua-demo`** - 빌드 설정 수정
5. **`hua-labs`** - 빌드 설정 수정
6. **`hua-ui`** - 빌드 설정 수정

### 장점 ✅
- 전체 코드베이스 일관성 유지
- CI/CD 파이프라인 완전 통과
- 향후 개발 시 즉시 사용 가능
- 코드 품질 향상
- 팀 협업 시 혼란 방지

### 단점 ❌
- 시간 소요 (예상: 1-2일)
- 일부 앱은 실제로 사용 안 할 수도 있음
- 불필요한 유지보수 부담

## 추천: 하이브리드 접근 (Hybrid)

### Phase 1: 명확히 불필요한 앱 삭제 (즉시)
1. **`hua-labs`** 삭제
   - 기본 Next.js 템플릿
   - 실제 사용 안 함
   - 복구 필요시 `create-next-app`으로 재생성 가능

### Phase 2: 메인 앱 빌드 수정 (우선순위 높음)
1. **`my-app`** 빌드 수정
   - 프로덕션 앱이므로 반드시 필요
   - 타입 에러 수정

### Phase 3: 개발 앱 빌드 수정 (선택적)
1. **`i18n-test`** - 개발 중이면 수정
2. **`hua-motion`** - 개발 중이면 수정

### Phase 4: 데모 앱 결정 (나중에)
1. **`hua-demo`** - 필요시 수정, 불필요시 삭제
2. **`hua-ui`** - Storybook과 중복 검토 후 결정

## 작업 시간 추정

### 옵션 1 (지우기)
- 삭제 작업: 10분
- 의존성 정리: 30분
- 총 시간: **~1시간**

### 옵션 2 (모두 수정)
- `my-app`: 30분
- `i18n-test`: 30분
- `hua-motion`: 1시간
- `hua-demo`: 30분
- `hua-labs`: 30분
- `hua-ui`: 30분
- 총 시간: **~3-4시간**

### 하이브리드 (추천)
- Phase 1 (삭제): 1시간
- Phase 2 (my-app): 30분
- Phase 3 (선택적): 1-2시간
- 총 시간: **~2-3시간**

## 최종 추천

**하이브리드 접근법**을 추천합니다:

1. **즉시 삭제**: `hua-labs` (명확히 불필요)
2. **필수 수정**: `my-app` (프로덕션 앱)
3. **선택적 수정**: 개발 중인 앱들만 필요시 수정
4. **나중에 결정**: 데모 앱들은 사용 목적 확인 후 결정

이렇게 하면:
- 불필요한 코드 제거 ✅
- 중요한 앱은 정상 작동 ✅
- 시간 효율적 ✅
- 유연한 대응 가능 ✅

