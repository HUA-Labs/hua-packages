# UI 패키지 서버/클라이언트 컴포넌트 분리 개선 계획

## 📋 개요

Next.js 16 App Router에서 발견된 UI 패키지의 서버/클라이언트 컴포넌트 분리 문제를 해결하기 위한 개선 계획입니다.

**작성일:** 2025-01-XX  
**관련 이슈:** Next.js 16 업그레이드 중 발견  
**대상 브랜치:** `feature/dashboard-ui-widgets` (예정)

---

## 🔍 현재 문제점

### 1. 모든 컴포넌트가 `"use client"`로 마크됨

**현재 상태:**
- UI 패키지의 거의 모든 컴포넌트가 `"use client"` 지시어를 사용
- 서버 컴포넌트에서 사용할 수 있는 컴포넌트가 거의 없음
- Next.js App Router의 서버 컴포넌트 이점을 활용하지 못함

**영향:**
- 불필요한 클라이언트 번들 크기 증가
- 서버 사이드 렌더링 최적화 기회 상실
- 서버 컴포넌트에서 직접 사용 불가

### 2. 클라이언트/서버 구분이 불명확

**현재 상태:**
- Context를 사용하는 컴포넌트(`Toast`, `ThemeProvider`)와 단순 UI 컴포넌트가 동일하게 export됨
- 사용자가 서버 컴포넌트에서 사용 가능한지 알기 어려움
- 빌드 시점에만 에러가 발생하여 개발 경험이 좋지 않음

**영향:**
- 개발자가 실수로 서버 컴포넌트에서 클라이언트 전용 컴포넌트를 사용
- 에러 발생 시점이 늦어 디버깅이 어려움

### 3. 임시 해결책의 한계

**현재 해결책:**
- 각 앱에서 클라이언트 래퍼 컴포넌트 생성 (`ClientComponents.tsx`)
- `Providers` 컴포넌트 내부에 클라이언트 컴포넌트 포함

**한계:**
- 각 앱마다 중복 코드 발생
- 패키지 레벨에서 해결되지 않음
- 유지보수 부담 증가

---

## 🎯 개선 목표

1. **명확한 서버/클라이언트 분리**
   - 서버 컴포넌트에서 사용 가능한 컴포넌트는 `"use client"` 제거
   - 클라이언트 전용 컴포넌트는 별도 export 경로 제공

2. **개발자 경험 개선**
   - 타입 레벨에서 서버/클라이언트 구분
   - 명확한 import 경로 제공
   - 문서화 강화

3. **번들 크기 최적화**
   - 서버 컴포넌트에서 사용 시 불필요한 클라이언트 코드 제거
   - Tree-shaking 최적화

---

## 📐 개선 방안

### 옵션 1: 서버/클라이언트 분리 (권장) ⭐

**구조:**
```typescript
// packages/hua-ui/src/index.ts (서버 컴포넌트용)
export { Button } from './components/Button'; // "use client" 제거
export { Card } from './components/Card';
export { Input } from './components/Input';
// ... 서버에서도 사용 가능한 컴포넌트들

// packages/hua-ui/src/client.ts (클라이언트 전용)
export { ToastProvider, useToast } from './components/Toast';
export { ThemeProvider } from './components/ThemeProvider';
export { ScrollToTop } from './components/ScrollToTop';
// ... Context나 hooks를 사용하는 컴포넌트들
```

**사용법:**
```tsx
// 서버 컴포넌트에서
import { Button, Card } from '@hua-labs/ui';

// 클라이언트 컴포넌트에서
import { ToastProvider, ScrollToTop } from '@hua-labs/ui/client';
```

**장점:**
- ✅ 명확한 구분
- ✅ 타입 안정성
- ✅ 서버 컴포넌트 이점 활용
- ✅ 개발자 경험 개선

**단점:**
- ⚠️ Breaking change (import 경로 변경 필요)
- ⚠️ 마이그레이션 작업 필요

### 옵션 2: 타입 기반 가이드

클라이언트 전용 컴포넌트에 타입 힌트 추가:
```typescript
/**
 * @clientOnly - This component requires client-side rendering
 * Use in a Client Component with "use client" directive
 */
export const ToastProvider = ...
```

**장점:**
- ✅ Breaking change 없음
- ✅ 문서화 개선

**단점:**
- ⚠️ 런타임 에러는 여전히 발생
- ⚠️ 타입 레벨 보호 부족

### 옵션 3: Wrapper 패턴

클라이언트 컴포넌트를 위한 서버 컴포넌트 래퍼 제공:
```typescript
// 서버 컴포넌트용 래퍼
export function ToastProviderWrapper({ children }) {
  return (
    <ClientToastProvider>
      {children}
    </ClientToastProvider>
  );
}
```

**장점:**
- ✅ Breaking change 없음
- ✅ 서버 컴포넌트에서 사용 가능

**단점:**
- ⚠️ 래퍼 컴포넌트 추가 필요
- ⚠️ 복잡도 증가

---

## 🚀 실행 계획

### Phase 1: 분석 및 분류 (1일)

1. **컴포넌트 분류**
   - 서버 컴포넌트로 사용 가능한 컴포넌트 식별
   - 클라이언트 전용 컴포넌트 식별
   - Context/hooks 사용 여부 확인

2. **의존성 분석**
   - 각 컴포넌트의 의존성 확인
   - 클라이언트 전용 기능 사용 여부 확인

### Phase 2: 구조 개선 (2-3일)

1. **서버 컴포넌트 최적화**
   - 불필요한 `"use client"` 제거
   - 서버에서 사용 가능한 컴포넌트 검증

2. **클라이언트 컴포넌트 분리**
   - `src/client.ts` 파일 생성
   - 클라이언트 전용 컴포넌트 export

3. **package.json exports 업데이트**
   - `./client` export 경로 추가
   - 타입 정의 업데이트

### Phase 3: 테스트 및 문서화 (1-2일)

1. **테스트 작성**
   - 서버 컴포넌트 사용 테스트
   - 클라이언트 컴포넌트 사용 테스트
   - 빌드 테스트

2. **문서화**
   - 사용 가이드 작성
   - 마이그레이션 가이드 작성
   - 예제 코드 추가

### Phase 4: 마이그레이션 (2-3일)

1. **앱별 마이그레이션**
   - `my-api` 마이그레이션
   - `my-chat` 마이그레이션
   - `my-app` 마이그레이션

2. **검증**
   - 빌드 테스트
   - 런타임 테스트
   - 성능 측정

---

## 📊 예상 작업량

| Phase | 작업 | 예상 시간 |
|-------|------|----------|
| Phase 1 | 분석 및 분류 | 1일 |
| Phase 2 | 구조 개선 | 2-3일 |
| Phase 3 | 테스트 및 문서화 | 1-2일 |
| Phase 4 | 마이그레이션 | 2-3일 |
| **총계** | | **6-9일** |

---

## ✅ 성공 지표

1. **기술적 지표**
   - ✅ 서버 컴포넌트에서 사용 가능한 컴포넌트 비율 70% 이상
   - ✅ 클라이언트 번들 크기 10% 이상 감소
   - ✅ 빌드 에러 0개

2. **개발자 경험**
   - ✅ 타입 레벨에서 서버/클라이언트 구분 가능
   - ✅ 명확한 import 경로 제공
   - ✅ 문서화 완료

---

## 🔗 관련 문서

- [IMPROVEMENT_PLAN_2025.md](./packages/hua-ui/docs/IMPROVEMENT_PLAN_2025.md) - UI 패키지 개선 계획
- [NEXTJS_16_VERIFICATION_GUIDE.md](./NEXTJS_16_VERIFICATION_GUIDE.md) - Next.js 16 검증 가이드

---

## 📝 참고사항

1. **Breaking Change 고려**
   - 마이그레이션 가이드 제공 필수
   - 버전 업그레이드 시 주의사항 명시

2. **하위 호환성**
   - 가능한 경우 기존 import 경로 유지
   - 점진적 마이그레이션 지원

3. **테스트 커버리지**
   - 서버/클라이언트 분리 테스트 추가
   - CI/CD 파이프라인에 통합

