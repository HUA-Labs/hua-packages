# Sum Diary UX/UI 리팩토링 플랜

## 개요

Sum Diary 앱의 전체 UX/UI를 hua-ux 프레임워크로 마이그레이션하고, 반응형 디자인을 적용하는 작업 계획.

**현재 상태:**
- 메인 페이지 (`/`): hua-ux 적용 완료
- 비즈니스 로직: 안정화됨
- UX/UI: 전면 리팩토링 필요

**목표:**
- hua-ux 프레임워크 컴포넌트로 통일
- 다국어 지원 (ko/en/ja)
- 모바일 우선 반응형 디자인
- 코드 스플리팅 최적화

---

## 페이지 인벤토리

### 사용자 페이지 (총 18개)

| 페이지 | 경로 | 우선순위 | 복잡도 | 상태 |
|--------|------|----------|--------|------|
| 메인/랜딩 | `/` | - | 중 | ✅ 완료 |
| 대시보드 | `/` (로그인) | 높음 | 높음 | ✅ 번역완료 |
| 로그인 | `/auth/login` | 높음 | 중 | ✅ 번역완료 |
| 회원가입 | `/auth/register` | 높음 | 높음 | ✅ 번역완료 |
| 비밀번호 찾기 | `/auth/forgot-password` | 중 | 낮음 | ✅ 번역완료 |
| 비밀번호 재설정 | `/auth/reset-password` | 중 | 낮음 | ✅ 번역완료 |
| 일기 목록 | `/diary` | 높음 | 중 | ✅ 번역완료 |
| 일기 작성 | `/diary/write` | 높음 | 높음 | ✅ 번역완료 |
| 일기 상세 | `/diary/[id]` | 높음 | 중 | ✅ 번역완료 |
| AI 분석 | `/diary/analysis` | 중 | 높음 | ✅ 번역완료 |
| 설정 | `/settings` | 중 | 중 | ✅ 번역완료 |
| 프로필 | `/profile` | 중 | 중 | 🔄 예정 |
| 알림 | `/notifications` | 낮음 | 낮음 | 🔄 예정 |
| 검색 | `/search` | 낮음 | 중 | 🔄 예정 |
| 공지사항 | `/announcements` | 낮음 | 낮음 | 🔄 예정 |
| 문의하기 | `/contact` | 낮음 | 낮음 | 🔄 예정 |
| 약관 | `/terms` | 낮음 | 낮음 | 🔄 예정 |
| 개인정보처리방침 | `/privacy` | 낮음 | 낮음 | 🔄 예정 |

### 관리자 페이지 (총 22개) - 🇰🇷 한국어만

> **참고:** 관리자 페이지는 한국어만 지원 (관리자가 한국인)

| 페이지 | 경로 | 우선순위 |
|--------|------|----------|
| 관리자 대시보드 | `/admin` | 중 |
| 사용자 관리 | `/admin/users/*` | 중 |
| 일기 관리 | `/admin/diaries/*` | 중 |
| 모니터링 | `/admin/monitoring/*` | 낮음 |
| 공지사항 관리 | `/admin/announcements/*` | 낮음 |
| 알림 관리 | `/admin/notifications/*` | 낮음 |

---

## 마이그레이션 단계

### Phase 1: 핵심 사용자 플로우 (1-2주)

**목표:** 로그인 → 일기 작성 → 일기 확인 플로우 완성

1. **Auth 페이지 리팩토링**
   - [ ] `/auth/login` - hua-ux 컴포넌트 적용
   - [ ] `/auth/register` - hua-ux 컴포넌트 + 번역 적용
   - [ ] 반응형 레이아웃

2. **대시보드 리팩토링**
   - [ ] `Dashboard.tsx` 컴포넌트 분리
   - [ ] 캘린더 컴포넌트 hua-ux 적용
   - [ ] 통계 카드 컴포넌트화

3. **일기 작성/상세 리팩토링**
   - [ ] `/diary/write` - 에디터 UI 개선
   - [ ] `/diary/[id]` - 상세 페이지 반응형
   - [ ] DraftModal 코드 스플리팅

### Phase 2: 보조 기능 (1주)

4. **일기 목록/검색**
   - [ ] `/diary` - 리스트 뷰 개선
   - [ ] `/search` - 검색 UI 개선

5. **설정/프로필**
   - [ ] `/settings` - 이미 번역 완료, 디자인만
   - [ ] `/profile` - 프로필 편집 UI

6. **AI 분석**
   - [ ] `/diary/analysis` - 결과 표시 UI

### Phase 3: 나머지 페이지 (1주)

7. **알림/공지**
   - [ ] `/notifications`
   - [ ] `/announcements`

8. **기타**
   - [ ] `/contact`
   - [ ] `/terms`, `/privacy`

### Phase 4: 관리자 페이지 (별도)

- 관리자 페이지는 사용자 페이지 완료 후 진행
- 내부용이므로 우선순위 낮음

---

## 기술 가이드라인

### 1. 컴포넌트 구조

```tsx
// 페이지 구조
'use client';

import { useTranslation } from '@hua-labs/i18n-core';
import { Button, Input, Panel } from '@hua-labs/ui';
import { SomeAdvancedComponent } from '@hua-labs/ui/advanced';

export default function SomePage() {
  const { t } = useTranslation();

  return (
    <div className="container mx-auto px-4">
      {/* 모바일 우선 반응형 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {/* ... */}
      </div>
    </div>
  );
}
```

### 2. 번역 적용

```tsx
// 번역 키 네이밍 규칙
t('namespace:section.key')

// 예시
t('auth:login.title')           // "로그인"
t('diary:write.placeholder')    // "오늘 하루를 기록해보세요"
t('common:button.save')         // "저장"
```

### 3. 반응형 브레이크포인트

```css
/* Tailwind 기본 브레이크포인트 사용 */
sm: 640px   /* 모바일 가로 */
md: 768px   /* 태블릿 */
lg: 1024px  /* 데스크톱 */
xl: 1280px  /* 대형 데스크톱 */
```

### 4. 코드 스플리팅

```tsx
// 모달, 차트 등 무거운 컴포넌트
import dynamic from 'next/dynamic';

const DraftModal = dynamic(() => import('./DraftModal'), {
  loading: () => <LoadingSpinner />,
});

// 설정 탭 등 조건부 렌더링
const ProfileTab = dynamic(() => import('./ProfileSettingsTab'));
```

### 5. 번역 파일 구조

```
translations/
├── ko/
│   ├── common.json      # 공통 (버튼, 에러 등)
│   ├── auth.json        # 인증 관련
│   ├── diary.json       # 일기 관련
│   ├── settings.json    # 설정 관련
│   └── ...
├── en/
│   └── ...
└── ja/
    └── ...
```

---

## 체크리스트

### 페이지별 체크리스트

각 페이지 마이그레이션 시 확인:

- [ ] hua-ux 컴포넌트 사용 (Button, Input, Panel 등)
- [ ] useTranslation 훅 적용
- [ ] 하드코딩된 한글 텍스트 제거
- [ ] 모바일 레이아웃 확인 (320px~)
- [ ] 태블릿 레이아웃 확인 (768px~)
- [ ] 데스크톱 레이아웃 확인 (1024px~)
- [ ] 다크모드 지원
- [ ] 로딩 상태 처리
- [ ] 에러 상태 처리
- [ ] 접근성 (키보드 네비게이션, aria-label)

### 공통 컴포넌트 체크리스트

- [ ] LoadingState → hua-ux 로딩 컴포넌트
- [ ] EmptyState → hua-ux 빈 상태 컴포넌트
- [ ] ErrorMessage → hua-ux 에러 컴포넌트
- [ ] Modal → hua-ux 모달 컴포넌트

---

## 현재 완료된 작업

### 번역 시스템
- [x] settings.json (ko/en/ja) - 설정 페이지 번역
- [x] auth.json (ko/en/ja) - 인증 페이지 번역
- [x] dashboard.json (ko/en/ja) - 대시보드 번역
- [x] diary.json (ko/en/ja) - 일기 관련 전체 번역

### Auth 페이지
- [x] auth/login 번역 적용
- [x] auth/forgot-password 번역 적용
- [x] auth/reset-password 번역 적용
- [x] auth/register 번역 적용
- [x] PasswordChecklist 컴포넌트 번역

### Diary 페이지
- [x] diary (목록) 번역 적용
- [x] diary/write 번역 적용 (DraftModal, hooks 포함)
- [x] diary/[id] (상세) 번역 적용
- [x] diary/analysis 번역 적용

### 설정 기능
- [x] settings/page.tsx 번역 적용
- [x] GeneralSettingsTab 번역 적용
- [x] 테마/언어 설정 DB 저장
- [x] 설정 자동 저장 기능

---

## 다음 작업

1. **프로필 페이지** - `/profile` 번역
2. **알림 페이지** - `/notifications` 번역
3. **검색 페이지** - `/search` 번역
4. **공지사항** - `/announcements` 번역
5. **기타** - `/contact`, `/terms`, `/privacy`

---

## 참고 사항

- hua-ux 컴포넌트 문서: `packages/hua-ux/README.md`
- UI 컴포넌트: `packages/hua-ui/src/components/`
- 번역 파일: `apps/my-app/app/lib/translations/`
