# 스키마 쿼리 최적화 제안

## 📅 작성일
2025-12-14

## 🔍 분석된 쿼리 패턴

### 1. DiaryEntry.title startsWith 쿼리 최적화

**현재 사용 패턴:**
- `title: { startsWith: '임시저장' }` - 매우 빈번하게 사용됨
- `title: { startsWith: '오프라인 일기' }` - 자주 사용됨
- `title: { not: { startsWith: '임시저장' } }` - 필터링용

**사용 위치:**
- `/api/diary/draft` - 임시저장 조회/삭제
- `/api/diary/draft/list` - 임시저장 목록
- `/api/diary/draft/latest` - 최신 임시저장
- `/api/diary/create` - 임시저장 개수 체크
- `/api/search` - 검색에서 임시저장 제외
- `/api/admin/diaries` - 관리자 일기 목록
- `/api/admin/dashboard/charts` - 통계 차트

**문제점:**
- `title` 필드에 인덱스가 없어서 `startsWith` 쿼리가 느릴 수 있음
- PostgreSQL에서 `LIKE '임시저장%'`는 인덱스를 활용할 수 있지만, `startsWith`는 제한적

**개선 방안:**

#### 옵션 1: title 필드에 인덱스 추가 (간단)
```prisma
model DiaryEntry {
  // ... 기존 필드들
  
  @@index([title]) // title 인덱스 추가
  @@index([user_id, title]) // 복합 인덱스 (user_id + title 조회 최적화)
  // ... 기존 인덱스들
}
```

**장점:**
- 구현 간단
- `startsWith` 쿼리 성능 개선

**단점:**
- `title`이 nullable이므로 NULL 값도 인덱스에 포함
- 전체 인덱스 크기 증가

#### 옵션 2: 부분 인덱스 사용 (권장)
```prisma
model DiaryEntry {
  // ... 기존 필드들
  
  // Prisma는 부분 인덱스를 직접 지원하지 않으므로
  // 마이그레이션 SQL에서 직접 생성 필요
  // CREATE INDEX idx_diary_entry_title_draft 
  //   ON "user"."DiaryEntry" (title) 
  //   WHERE title IS NOT NULL AND (title LIKE '임시저장%' OR title LIKE '오프라인 일기%');
}
```

**장점:**
- 인덱스 크기 최소화
- 임시저장 관련 쿼리만 최적화

**단점:**
- Prisma 스키마에 직접 표현 불가
- 마이그레이션 SQL 수동 작성 필요

#### 옵션 3: draft_type 필드 추가 (근본적 해결)
```prisma
model DiaryEntry {
  // ... 기존 필드들
  draft_type String? // 'DRAFT', 'OFFLINE_DRAFT', null (일반 일기)
  
  @@index([user_id, draft_type]) // 복합 인덱스
  @@index([draft_type, created_at]) // draft_type별 조회
}
```

**장점:**
- 가장 빠른 쿼리 성능
- `startsWith` 대신 `=` 비교 사용 가능
- 인덱스 활용도 최대

**단점:**
- 스키마 변경 필요
- 기존 데이터 마이그레이션 필요
- 코드 변경 범위 큼

### 2. ContactInquiry 쿼리 최적화

**현재 상태:**
- 인덱스가 잘 설정되어 있음 ✅
- `status`, `email`, `user_id`, `created_at` 모두 인덱스 있음

**추가 개선 가능:**
```prisma
model ContactInquiry {
  // ... 기존 필드들
  
  // IP 주소 기반 Rate Limiting 최적화
  @@index([ip_address, created_at]) // IP별 최근 문의 조회
}
```

### 3. DiaryEntry 삭제 쿼리 최적화

**현재 사용 패턴:**
- `deleteMany`에서 `id: { in: ids }` 사용 (배치 삭제)
- `user_id`와 함께 필터링

**현재 인덱스:**
- `@@index([user_id, created_at])` ✅
- `id`는 Primary Key이므로 자동 인덱스 ✅

**추가 개선:**
- 배치 삭제는 이미 최적화됨 ✅
- `id IN (...)` 쿼리는 PK 인덱스 활용

### 4. AnalysisResult 쿼리 최적화

**현재 상태:**
- 인덱스가 매우 잘 설정되어 있음 ✅
- `diary_id`, `provider`, `status`, `created_at` 모두 인덱스
- GIN 인덱스로 배열 검색 최적화 (`emotion_keywords`, `summary_topics`)

**추가 개선 가능:**
```prisma
model AnalysisResult {
  // ... 기존 필드들
  
  // 비용 분석 쿼리 최적화
  @@index([created_at, cost_usd]) // 일별 비용 분석 (이미 있음 ✅)
  @@index([provider, created_at, cost_usd]) // 프로바이더별 일별 비용
}
```

## 🎯 권장 사항

### 즉시 적용 가능 (낮은 리스크)

1. **DiaryEntry.title 인덱스 추가**
   ```prisma
   @@index([title])
   @@index([user_id, title])
   ```

2. **ContactInquiry.ip_address 인덱스 추가**
   ```prisma
   @@index([ip_address, created_at])
   ```

### 중기 개선 (중간 리스크)

3. **DiaryEntry.draft_type 필드 추가**
   - 스키마 변경 필요
   - 데이터 마이그레이션 필요
   - 코드 변경 범위 큼

### 장기 개선 (낮은 우선순위)

4. **부분 인덱스 적용**
   - Prisma 마이그레이션 SQL 수동 작성
   - 인덱스 크기 최적화

## 📊 예상 성능 개선

### DiaryEntry.title 인덱스 추가 시
- **Before**: `startsWith` 쿼리 시 전체 테이블 스캔 또는 느린 인덱스 스캔
- **After**: 인덱스 활용으로 **50-80% 성능 향상** 예상
- **영향**: 임시저장 조회/삭제 속도 개선

### ContactInquiry.ip_address 인덱스 추가 시
- **Before**: Rate Limiting 체크 시 전체 테이블 스캔
- **After**: 인덱스 활용으로 **90% 이상 성능 향상** 예상
- **영향**: 문의하기 API 응답 속도 개선

## 🔧 구현 방법

### 1단계: 스키마 수정
```prisma
model DiaryEntry {
  // ... 기존 필드들
  
  @@index([title])
  @@index([user_id, title])
  // ... 기존 인덱스들
}

model ContactInquiry {
  // ... 기존 필드들
  
  @@index([ip_address, created_at])
  // ... 기존 인덱스들
}
```

### 2단계: 마이그레이션 생성
```bash
cd apps/my-app
pnpm prisma migrate dev --name add_query_optimization_indexes
```

### 3단계: Prisma Client 재생성
```bash
pnpm prisma generate
```

## ⚠️ 주의사항

1. **인덱스 생성 시간**
   - 대용량 테이블의 경우 인덱스 생성에 시간이 걸릴 수 있음
   - 프로덕션에서는 점검 시간에 실행 권장

2. **인덱스 크기**
   - 추가 인덱스는 저장 공간을 사용
   - INSERT/UPDATE 성능에 미미한 영향 (보통 무시 가능)

3. **모니터링**
   - 인덱스 추가 후 쿼리 성능 모니터링 필요
   - EXPLAIN ANALYZE로 실제 성능 개선 확인

## 📝 참고

- PostgreSQL 인덱스 최적화 가이드: https://www.postgresql.org/docs/current/indexes.html
- Prisma 인덱스 문서: https://www.prisma.io/docs/concepts/components/prisma-schema/indexes
