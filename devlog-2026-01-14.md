# Devlog 2026-01-14

## 오늘의 목표
- [x] ui-pro와 advanced 패키지 정리 통합
- [x] hua-docs 사이트 전체 개선
- [x] 퍼블릭 레포 동기화 확인

---

## 패키지 구조 정리

### 현재 상태 분석

**중복 구조 발견:**
```
packages/hua-ui/
├── src/advanced/               # Logo 컴포넌트
├── src/components/advanced/    # 프리미엄 비주얼 효과 (Parallax, GlowCard 등)
└── src/components/dashboard/   # 대시보드 컴포넌트 (최신)

packages/hua-ui-pro/            # 별도 패키지 (중복!)
├── src/components/dashboard/   # 대시보드 (구버전, BarChart 없음)
└── src/components/Emotion*.tsx # Emotion 컴포넌트들
```

**문제점:**
1. dashboard 컴포넌트가 두 곳에 존재 (hua-ui가 최신)
2. ui-pro 패키지가 불필요하게 분리됨
3. 관리 복잡성 증가

### 정리 전략

**목표 구조 (완료):**
```
packages/hua-ui/
├── src/advanced/               # Logo
├── src/components/advanced/
│   ├── (motion components)     # Parallax, GlowCard, Marquee 등
│   └── emotion/                # Emotion 컴포넌트 (신규 이동)
│       ├── EmotionAnalysis.tsx
│       ├── EmotionButton.tsx
│       ├── EmotionMeter.tsx
│       ├── EmotionSelector.tsx
│       └── index.ts
├── src/components/dashboard/   # 대시보드 컴포넌트
└── src/components/             # 기본 컴포넌트 (공개)
```

---

## 작업 내역

### 1. Emotion 컴포넌트 hua-ui로 이동

**새 파일 생성:**
- `packages/hua-ui/src/components/advanced/emotion/EmotionAnalysis.tsx`
- `packages/hua-ui/src/components/advanced/emotion/EmotionButton.tsx`
- `packages/hua-ui/src/components/advanced/emotion/EmotionMeter.tsx`
- `packages/hua-ui/src/components/advanced/emotion/EmotionSelector.tsx`
- `packages/hua-ui/src/components/advanced/emotion/index.ts`

**import 경로 수정:**
- `@hua-labs/ui` → `../../../lib/utils` (merge 함수)
- `@hua-labs/ui` → `../../Card`, `../../Badge` (컴포넌트)

### 2. ui-pro 패키지 삭제

**삭제됨:**
- `packages/hua-ui-pro/` 전체 삭제
- dashboard 컴포넌트는 이미 hua-ui에 최신 버전 존재

### 3. tsup 빌드 설정 업데이트

**tsup.config.ts에 entry 추가:**
```typescript
'advanced-emotion': 'src/components/advanced/emotion/index.ts',
```

**package.json exports 추가:**
```json
"./advanced/emotion": {
  "types": "./dist/advanced-emotion.d.ts",
  "import": "./dist/advanced-emotion.mjs",
  "require": "./dist/advanced-emotion.js"
}
```

### 4. sync 스크립트 업데이트

**scripts/sync-to-public.js 수정:**
```javascript
exclude: [
  // ... 기존 제외 항목
  // Emotion Analysis Components (private only)
  'src/components/advanced/emotion/**/*',
]
```

### 5. hua-docs 패키지 정보 업데이트

**constants/packages.ts 전면 업데이트:**
- `npmVersion` 필드 추가 (npm 배포 버전)
- `status` 필드 추가 (`stable` | `alpha` | `beta` | `wip` | `coming-soon`)
- `isPublished` 필드 추가 (npm 배포 여부)

**버전 정보 현황:**

| 패키지 | npm 배포 | 로컬 | 상태 |
|--------|----------|------|------|
| @hua-labs/hua-ux | 0.1.0-alpha.18 | 0.1.0-alpha.12 | alpha |
| @hua-labs/ui | 1.0.0 | 1.1.0-alpha.1 | alpha |
| @hua-labs/motion-core | 2.0.0 | 2.1.0-alpha.3 | alpha |
| @hua-labs/i18n-core | 1.0.0 | 1.1.0-alpha.3 | alpha |
| @hua-labs/state | 1.0.0-alpha.7 | 1.0.0-alpha.5 | alpha |
| @hua-labs/i18n-core-zustand | 1.0.0 | 1.1.0-alpha.1 | alpha |
| @hua-labs/i18n-loaders | 1.0.0 | 1.1.0-alpha.4 | alpha |
| create-hua-ux | 1.0.0-alpha.19 | 1.0.0-alpha.15 | alpha |

**패키지 카드 UI 개선:**
- Alpha 뱃지 추가 (보라색)
- WIP 뱃지 추가 (노란색)
- Local 뱃지 추가 (미배포 패키지)
- npm 버전 우선 표시

### 6. advanced 컴포넌트 구조

**@hua-labs/ui/advanced exports:**
```typescript
// Visual Effects (Premium)
export { Parallax } from "./Parallax";
export { Marquee } from "./Marquee";
export { GlowCard } from "./GlowCard";
export { SpotlightCard } from "./SpotlightCard";
export { TextReveal } from "./TextReveal";
export { AnimatedGradient } from "./AnimatedGradient";
export { TiltCard } from "./TiltCard";
export { VideoBackground } from "./VideoBackground";
export { Carousel } from "./Carousel";

// Emotion Analysis (Premium)
export * from "./emotion";
```

---

## 빌드 확인

### hua-ui 빌드 ✅
```bash
pnpm build
# 368 files processed
# advanced-emotion.mjs 생성 확인
```

### hua-docs 빌드 ✅
```bash
pnpm build
# 모든 페이지 정상 빌드
```

---

## 문서 사이트 개선 (2차)

### 7. 컴포넌트 레지스트리 구축

**constants/component-registry.ts 생성:**
- 71개 컴포넌트 메타데이터 중앙 관리
- 7개 카테고리: form, layout, feedback, overlay, navigation, data, advanced
- Props 정의 포함
- hasDoc 플래그로 문서화 상태 추적

```typescript
export interface ComponentDef {
  id: string;
  name: string;
  description: string;
  descriptionKo: string;
  category: ComponentCategory;
  status: ComponentStatus;
  hasDoc: boolean;
  props?: PropDef[];
}
```

### 8. i18n 도입 (문서 사이트)

**번역 파일 추가:**
- `public/translations/ko/docs.json`
- `public/translations/en/docs.json`

**기존 isKo 조건 분기 → t() 함수 전환:**
```typescript
// Before
{isKo ? "라이브 데모" : "Live Demo"}

// After
{t("docs:common.liveDemo")}
```

**네임스페이스 구조:**
- `common`: 네비게이션, 푸터 등 공통
- `docs`: 컴포넌트 문서 전용
- `packages`: 패키지 문서 전용

### 9. 문서 템플릿 시스템

**ComponentDoc.tsx 유틸 컴포넌트 추가:**
- `CodeExample`: 코드 스니펫 + 복사 버튼
- `DocNavigation`: Prev/Next 네비게이션
- `ImportSection`: import 문 표시
- `InstallSection`: 설치 명령어 표시
- `StatusBadge`: Beta/Alpha/Coming Soon 뱃지

### 10. 컴포넌트 문서 페이지 추가

**신규 문서 페이지:**
- `app/docs/components/number-input/page.tsx` (NumberInput)
- `app/docs/components/form-control/page.tsx` (FormControl + useFormValidation)

**문서 페이지 구조:**
1. 헤더 (이름, 설명, 패키지, 버전)
2. Import 섹션
3. 라이브 데모 (인터랙티브)
4. Props 테이블
5. 코드 예시
6. 접근성 정보
7. Prev/Next 네비게이션

---

## 다음 작업 (TODO)

- [ ] npm 배포 버전 동기화 (로컬이 낮은 경우)
- [ ] i18n-sdk 패키지 문서 페이지 추가
- [x] 컴포넌트 문서 페이지 추가 (NumberInput, FormControl)
- [ ] 추가 컴포넌트 문서화 (Select, Modal, Toast 등)
- [ ] advanced/emotion 사용 예제 문서 추가
- [ ] 사이드바 네비게이션 동적 생성 (component-registry 활용)
- [x] ComponentDoc 컴포넌트 i18n 완전 전환

---

## i18n 번역 작업 (2차)

### 11. docs.json 번역 키 확장

**영어/한국어 번역 파일 업데이트:**
- `public/translations/en/docs.json`
- `public/translations/ko/docs.json`

**추가된 컴포넌트 번역:**
- `alert`: variants, demo texts, convenience components
- `badge`: status examples, performance note, glass variant
- `input`: demo texts, states
- `parallax`: demo texts

### 12. 문서 페이지 i18n 전환

**isKo 조건 → t() 함수 전환 완료:**
- `app/docs/components/alert/page.tsx`
- `app/docs/components/badge/page.tsx`
- `app/docs/components/input/page.tsx`
- `app/docs/components/parallax/page.tsx`

**이미 완료된 페이지:**
- `app/docs/components/button/page.tsx`
- `app/docs/components/card/page.tsx`

### 13. 번역 구조

**네임스페이스:**
```
common:     공통 UI 텍스트 (nav, footer, actions 등)
docs:       문서 전용 텍스트
  - common: 문서 공통 (liveDemo, props, variants 등)
  - components.[name]: 컴포넌트별 번역
  - status: 상태 뱃지 텍스트
  - props: Props 테이블 헤더
```

---

## 발견된 이슈

### npm 버전 불일치

**@hua-labs/ui 버전 상황:**
```
npm 배포: 1.0.0, 1.1.0-alpha.*, 2.0.0-alpha.7~9
로컬: 1.1.0-alpha.1
```

누군가 이미 2.0.0-alpha 버전을 npm에 배포했음.
로컬 버전이 npm보다 뒤처져 있는 상황.

**대응 방안:**
1. 로컬 버전을 2.0.0-alpha.10으로 올리고 계속 진행 ✅ 선택
2. 2.0.0-alpha.* 버전 deprecate 처리 후 1.x 라인 유지

---

## 컴포넌트 문서화 작업 (3차)

### 현황 분석

**총 컴포넌트 수:** 71개 (레지스트리 기준)
**문서화 완료:** 11개
**문서화 필요:** 60개

### 문서화 완료 (hasDoc: true)
| 컴포넌트 | 카테고리 |
|---------|---------|
| Input | form |
| NumberInput | form |
| FormControl | form |
| Card | layout |
| Alert | feedback |
| Badge | feedback |
| Button | navigation |
| Parallax | advanced |
| Marquee | advanced |
| GlowCard | advanced |
| Carousel | advanced |
| VideoBackground | advanced |

### 문서화 필요 (우선순위순)

**🔴 높음 - 자주 사용되는 기본 컴포넌트**
1. Modal - 가장 많이 쓰이는 오버레이
2. Toast - 알림 시스템
3. Select - 폼 필수 요소
4. Tabs - 네비게이션 필수
5. Checkbox/Radio/Switch - 폼 필수

**🟡 중간 - 일반적인 컴포넌트**
6. Drawer - 사이드 패널
7. Tooltip - 힌트 표시
8. Progress/Skeleton - 로딩 상태
9. Textarea - 텍스트 입력
10. Avatar - 사용자 표시

**🟢 낮음 - 특수 목적**
11. DatePicker, ColorPicker - 특수 입력
12. Autocomplete - 검색 기능
13. Table, Accordion - 데이터 표시
14. Command - 커맨드 팔레트
15. Dashboard 컴포넌트들

### 레지스트리에 누락된 컴포넌트
- Action, ActionToolbar
- Panel
- ChatMessage
- FeatureCard, InfoCard
- ThemeToggle, ThemeProvider
- LanguageToggle
- ScrollProgress, ScrollToTop, ScrollArea
- PageTransition, PageNavigation
- Icon, IconProvider
- Dashboard 컴포넌트 (20개+)

---

## 컴포넌트 문서화 진행 (4차)

### 14. Modal 문서 페이지 생성

**파일:** `app/docs/components/modal/page.tsx`

**기능:**
- 기본 모달 데모
- 제목/설명 포함 모달
- 다양한 크기 (sm, md, lg, xl)
- 폼 모달 예시
- Props 테이블
- 접근성 정보 (role, aria, keyboard, scroll lock)

### 15. ComponentDocHeader 미배포 표시 기능

**추가된 props:**
```typescript
isPublished?: boolean; // npm에 배포됨 (default: true)
status?: 'stable' | 'beta' | 'alpha' | 'deprecated' | 'coming-soon';
```

**새 컴포넌트:**
- `UnpublishedBanner`: 미배포 컴포넌트용 정보 배너 (파란색)

**사용법:**
```tsx
<ComponentDocHeader
  name="NewComponent"
  isPublished={false}  // "업데이트 예정" 뱃지 + 배너 표시
  status="beta"        // Beta 뱃지 표시
  ...
/>
```

### 16. 번역 파일 업데이트

**Modal 번역 추가 (en/ko):**
- `docs:components.modal.demo.basic`
- `docs:components.modal.demo.openModal`
- `docs:components.modal.demo.basicContent`
- `docs:components.modal.demo.withTitle`
- `docs:components.modal.demo.confirmTitle`
- `docs:components.modal.demo.sizes`
- `docs:components.modal.demo.formModal`

**Toast 번역 추가 (en/ko):**
- `docs:components.toast.demo.types`
- `docs:components.toast.demo.success/error/warning/info`
- `docs:components.toast.demo.withTitle`
- `docs:components.toast.demo.withAction`

---

## 메모

### 패키지 배포 전략

**공개 패키지 (퍼블릭 레포):**
- @hua-labs/ui (기본 컴포넌트만)
- @hua-labs/motion-core
- @hua-labs/i18n-core
- @hua-labs/state
- create-hua-ux

**프리미엄 패키지 (프라이빗만):**
- @hua-labs/ui/advanced (모션 효과)
- @hua-labs/ui/advanced/emotion (감정 분석)
- @hua-labs/ui/advanced/dashboard (대시보드)
- @hua-labs/motion-advanced

---

## 네비게이션 개선 (5차)

### 17. Hook 페이지 뒤로가기/네비게이션 추가

**Hooks 인덱스 페이지:**
- `/docs/hooks/page.tsx`에 "문서로 돌아가기" 링크 추가

**개별 Hook 페이지 업데이트:**
- `use-in-view/page.tsx` - Back + Next(useScrollProgress)
- `use-scroll-progress/page.tsx` - Back + Prev/Next
- `use-mouse/page.tsx` - Back + Prev/Next
- `use-window-size/page.tsx` - Back + Prev/Next
- `use-reduced-motion/page.tsx` - Back + Prev(useWindowSize)

**네비게이션 순서:**
```
useInView → useScrollProgress → useMouse → useWindowSize → useReducedMotion
```

**추가 사항:**
- ArrowLeft, ArrowRight 아이콘 import 추가
- 한/영 텍스트 지원 ("훅 목록으로 돌아가기" / "Back to Hooks")
- 기존 컴포넌트 문서 페이지와 동일한 스타일 적용

---

## 페이지별 네비게이션 현황

| 섹션 | 뒤로가기 | Prev/Next |
|------|----------|-----------|
| 가이드 페이지 | ✅ | ✅ |
| 컴포넌트 문서 | ✅ | ✅ |
| Hook 페이지 | ✅ | ✅ |
| 패키지 페이지 | - | - |

---

## 공통 컴포넌트 추가 (6차)

### 18. BackLink 컴포넌트 추가

**파일:** `components/docs/ComponentDoc.tsx`

```typescript
interface BackLinkProps {
  href: string;
  label?: string;
  labelKo?: string;
}

export function BackLink({ href, label = "Back", labelKo }: BackLinkProps)
```

**사용법:**
```tsx
import { BackLink } from "@/components/docs/ComponentDoc";

<BackLink href="/docs/components" label="Back to Components" labelKo="컴포넌트로 돌아가기" />
```

---

## Select 문서 페이지 (7차)

### 19. Select 문서 페이지 생성

**파일:** `app/docs/components/select/page.tsx`

**기능:**
- 기본 Select 데모
- 플레이스홀더 사용
- 제어 모드 (controlled)
- 5가지 변형 (default, outline, filled, ghost, glass)
- 3가지 크기 (sm, md, lg)
- 상태 (error, success, disabled)
- 아이콘 포함

**번역 키 추가:**
- `docs:components.select.demo.*`
- `docs:components.select.accessibility.*`

**레지스트리 업데이트:**
- `select` hasDoc: true

---

## Checkbox/Radio/Switch 문서 페이지 (8차)

### 20. Checkbox 문서 페이지 생성

**파일:** `app/docs/components/checkbox/page.tsx`

**기능:**
- 기본 체크박스 데모
- 설명 포함 체크박스
- 제어 모드 (controlled)
- 4가지 변형 (default, outline, filled, glass)
- 3가지 크기 (sm, md, lg)
- 상태 (error, success, disabled)

**i18n 번역 키:**
- `docs:components.checkbox.demo.*`
- `docs:components.checkbox.accessibility.*`

### 21. Radio 문서 페이지 생성

**파일:** `app/docs/components/radio/page.tsx`

**기능:**
- 라디오 그룹 데모 (Plan 선택)
- 수평 레이아웃
- 4가지 변형 (default, outline, filled, glass)
- 3가지 크기 (sm, md, lg)
- 상태 (error, success, disabled)

**i18n 번역 키:**
- `docs:components.radio.demo.*`
- `docs:components.radio.accessibility.*`

### 22. Switch 문서 페이지 생성

**파일:** `app/docs/components/switch/page.tsx`

**기능:**
- 기본 스위치 데모
- 설명 포함 스위치
- 제어 모드 (Dark Mode 토글)
- 4가지 변형 (default, outline, filled, glass)
- 3가지 크기 (sm, md, lg)
- 상태 (error, success, disabled)

**i18n 번역 키:**
- `docs:components.switch.demo.*`
- `docs:components.switch.accessibility.*`

### 23. 레지스트리 업데이트

**component-registry.ts 수정:**
- `checkbox` hasDoc: true
- `radio` hasDoc: true
- `switch` hasDoc: true

### 24. 번역 파일 업데이트

**en/docs.json, ko/docs.json 업데이트:**
- Checkbox, Radio, Switch 데모/접근성 텍스트 추가
- 컴포넌트별 번역 키 구조화

---

## 문서화 현황 업데이트

**총 컴포넌트 수:** 71개 (레지스트리 기준)
**문서화 완료:** 15개 (+4)
**문서화 필요:** 56개

| 컴포넌트 | 카테고리 | 상태 |
|---------|---------|------|
| Input | form | ✅ |
| NumberInput | form | ✅ |
| FormControl | form | ✅ |
| Select | form | ✅ |
| Checkbox | form | ✅ New |
| Radio | form | ✅ New |
| Switch | form | ✅ New |
| Card | layout | ✅ |
| Alert | feedback | ✅ |
| Badge | feedback | ✅ |
| Modal | overlay | ✅ |
| Button | navigation | ✅ |
| Parallax | advanced | ✅ |
| Marquee | advanced | ✅ |
| GlowCard | advanced | ✅ |
| Carousel | advanced | ✅ |
| VideoBackground | advanced | ✅ |

---

## 문서 템플릿 시스템 구축 (9차)

### 25. 데모 섹션 공통 컴포넌트 추가

**파일:** `components/docs/ComponentDoc.tsx`

**새 컴포넌트:**
```typescript
// 데모 섹션 래퍼
DemoSection      // 라이브 데모 전체를 감싸는 Card
DemoBlock        // 각 데모 블록 (title + children)

// 변형/크기/상태 쇼케이스
VariantShowcase  // 변형 데모 그리드 (columns: 2|3|4)
VariantItem      // 변형 아이템 (name + component, darkBg option)
SizeShowcase     // 크기 데모 리스트
SizeItem         // 크기 아이템 (size label + component)
StateShowcase    // 상태 데모 리스트

// 유틸리티
ValueDisplay     // controlled 값 표시 (label + Badge)
```

### 26. ComponentDocTemplate 표준 템플릿 생성

**파일:** `templates/ComponentDocTemplate.tsx`

**목적:**
- 새 문서 페이지 생성 시 복사해서 사용
- 일관된 페이지 구조 유지
- TODO 주석으로 수정해야 할 부분 명시

**페이지 구조:**
1. BackLink (뒤로가기)
2. ComponentDocHeader (이름, 설명, 패키지)
3. DemoSection
   - DemoBlock (기본)
   - DemoBlock (controlled)
   - DemoBlock (variants) + VariantShowcase
   - DemoBlock (sizes) + SizeShowcase
   - DemoBlock (states) + StateShowcase
4. PropsTable
5. ExampleSection + CodeExample
6. AccessibilitySection
7. DocNavigation (이전/다음)

### 27. 라이트/다크 모드 가이드라인

**권장 색상:**
```
배경:   bg-background, bg-card, bg-secondary
텍스트: text-foreground, text-muted-foreground
보더:   border-border
강조:   text-primary
```

**피해야 할 것:**
- 하드코딩된 색상 (text-gray-500 대신 text-muted-foreground)
- white/black 직접 사용 (dark mode에서 깨짐)

**예외:**
- glass variant 배경: `darkBg` prop 사용
- GlowCard 데모: 어두운 배경에서 효과 보여주기 위한 의도적 사용

### 28. 기존 페이지 현황

**라이트/다크 모드 지원 상태:**
- 대부분 `text-gray-600 dark:text-gray-400` 패턴 사용 중
- 기능적으로는 정상 작동
- `text-muted-foreground`로 통일하면 더 일관됨

**리팩토링 대상:**
- 새 공통 컴포넌트 사용으로 전환 가능
- 우선순위 낮음 (기능에는 문제 없음)

---

## 추가 컴포넌트 문서화 (10차)

### 29. Slider 문서 페이지 생성

**파일:** `app/docs/components/slider/page.tsx`

**기능:**
- 기본 슬라이더
- 값 표시 (showValue)
- 레이블 포함 (showLabel)
- 범위 슬라이더 (value를 배열로)
- 5가지 변형 (default, primary, success, warning, danger)
- 3가지 크기 (sm, md, lg)
- 세로 방향 (orientation="vertical")

### 30. Textarea 문서 페이지 생성

**파일:** `app/docs/components/textarea/page.tsx`

**기능:**
- 기본 텍스트영역
- 제어 모드 + 글자 수 표시
- 5가지 변형 (default, outline, filled, ghost, glass)
- 3가지 크기 (sm, md, lg)
- 크기 조절 옵션 (resize: none, vertical, horizontal, both)
- 상태 (normal, error, success, disabled)

### 31. 레지스트리 업데이트

**component-registry.ts 수정:**
- `slider` hasDoc: true
- `textarea` hasDoc: true

---

## 문서화 현황 최종

**총 컴포넌트 수:** 71개 (레지스트리 기준)
**문서화 완료:** 17개 (+2)

| 컴포넌트 | 카테고리 | 상태 |
|---------|---------|------|
| Input | form | ✅ |
| NumberInput | form | ✅ |
| FormControl | form | ✅ |
| Select | form | ✅ |
| Checkbox | form | ✅ |
| Radio | form | ✅ |
| Switch | form | ✅ |
| Slider | form | ✅ New |
| Textarea | form | ✅ New |
| Card | layout | ✅ |
| Alert | feedback | ✅ |
| Badge | feedback | ✅ |
| Toast | feedback | ✅ |
| Modal | overlay | ✅ |
| Button | navigation | ✅ |
| Tabs | navigation | ✅ |
| Parallax | advanced | ✅ |
| Marquee | advanced | ✅ |
| GlowCard | advanced | ✅ |
| Carousel | advanced | ✅ |
| VideoBackground | advanced | ✅ |

---

## 다음 작업 (TODO 업데이트)

- [x] Select 문서 페이지 생성
- [x] Checkbox/Radio/Switch 문서 페이지 생성
- [x] BackLink 공통 컴포넌트 추출
- [x] 데모 섹션 공통 컴포넌트 추가
- [x] ComponentDocTemplate 표준 템플릿 생성
- [x] Slider 문서 페이지 생성
- [x] Textarea 문서 페이지 생성
- [x] 기존 페이지들 새 컴포넌트로 리팩토링 (선택적)
- [ ] Drawer, Tooltip 문서 페이지 생성
- [ ] Progress, Skeleton 문서 페이지 생성

---

## Advanced 컴포넌트 문서 서식 통일 (11차)

### 32. 기존 페이지 새 서식 적용

**업데이트된 페이지:**
- `app/docs/components/parallax/page.tsx`
- `app/docs/components/marquee/page.tsx`
- `app/docs/components/glow-card/page.tsx`
- `app/docs/components/carousel/page.tsx`
- `app/docs/components/video-background/page.tsx`

**적용된 변경사항:**
1. `BackLink` 추가 (컴포넌트 목록으로 돌아가기)
2. `useMotion` fadeIn 애니메이션 추가
3. `DemoSection`, `DemoBlock` 구조 적용
4. `CodeExample` 컴포넌트로 코드 예시 표시
5. `DocNavigation` 이전/다음 네비게이션 추가
6. `accessibilityItems` 배열로 분리

**VideoBackground:**
- 실제 `VideoBackground` 컴포넌트로 유튜브 영상 재생
- YouTube ID: `W0LHTWG-UmQ` (ISS Earth Timelapse)

### 33. Carousel 높이 문제 해결

**문제:**
- `h-full` 적용해도 인디케이터/버튼 높이만큼만 표시됨
- `slide` transition일 때만 문제 발생

**해결:**
- `aspect-ratio: 16/9` + `min-h-[300px]` 조합 적용
- 컨테이너와 Carousel 모두에 min-height 명시

### 34. Carousel 무한 루프 개선

**문제:**
- 마지막 슬라이드 → 첫 슬라이드 이동 시 역방향 애니메이션

**해결 (slide transition):**
1. 슬라이드 복제: `[마지막, ...원본, 첫번째]`
2. 시작 인덱스 1로 설정 (복제된 마지막 다음)
3. 끝에서 처음으로 갈 때: 복제된 슬라이드로 자연스럽게 이동 후 점프
4. `noTransition` 상태로 점프 시 애니메이션 제거

**수정된 파일:**
- `packages/hua-ui/src/components/advanced/Carousel.tsx`

**주요 변경:**
```typescript
// 슬라이드 복제 (loop mode에서 slide transition일 때)
const slides = loop
  ? [childArray[childArray.length - 1], ...childArray, childArray[0]]
  : childArray;

// 복제 슬라이드 도달 시 실제 슬라이드로 점프
useEffect(() => {
  if (currentIndex === 0) {
    // 복제된 마지막 → 실제 마지막으로 점프
    setNoTransition(true);
    setCurrentIndex(slideCount);
  } else if (currentIndex === slideCount + 1) {
    // 복제된 첫번째 → 실제 첫번째로 점프
    setNoTransition(true);
    setCurrentIndex(1);
  }
}, [currentIndex]);
```

**인디케이터 수정:**
- `getActualIndex()` 함수로 실제 인덱스 계산
- 인디케이터 클릭 시 loop 모드면 +1 오프셋 적용
